=== Promotion Evidence ===
Migration promoted from dev-retention-schema to production
Timestamp: 2026-08-28T00:45:00Z

Statements applied to production:
  ALTER TABLE retention_actions ADD COLUMN IF NOT EXISTS priority_score NUMERIC(5,2) DEFAULT 0.0
  CREATE INDEX IF NOT EXISTS idx_retention_actions_subscriber_outcome ON retention_actions (subscriber_id, outcome, created_at DESC)
  UPDATE retention_actions SET priority_score = CASE ... END

Verification: SELECT confirmed priority_score column exists on production
Production now matches dev schema for retention_actions table
