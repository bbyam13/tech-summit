/**
 * Streamline Telco dashboard routes (stub).
 *
 * TODO: Adapt from Streamline domain (subscriber metrics, risk KPIs, etc.).
 * For now, this is a placeholder to prevent import errors.
 */

import type { Application } from 'express';
import type { AppDb } from '../db/index.js';

export function registerDashboardRoutes(_app: Application, _deps: { db: AppDb }): void {
  // Placeholder — implement per Streamline domain.
}
