/**
 * Streamline Telco Care Desk routes (stub).
 *
 * TODO: Adapt from Streamline domain (subscribers, care actions, offers).
 * For now, this is a placeholder to prevent import errors.
 */

import type { Application } from 'express';
import type { AppDb } from '../db/index.js';

export function registerCareRoutes(_app: Application, _deps: { db: AppDb }): void {
  // Placeholder — implement per Streamline domain.
}
